import baseObj from '../../config/_base'

const { base, redirect } = baseObj

const config = {
    google: {
        label: 'Google',
        href: `${base}`,
        redirect: redirect
    }
}

export default config;